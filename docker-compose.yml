version: "3"
services:
  app:
    container_name: app
    restart: always
    build: .
    ports:
      - 80:80
    links:
      - mongo
      - nginx
    volumes:
      - ./app:/AppData
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - ./data:/data/db
  nginx:
    container_name: nginx
    image: nginx
    restart: always